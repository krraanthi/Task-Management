[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "B"]
ignore = ["E501"] # Line too long (already handled by formatter)

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
